# Verto — offline file converter (no network libraries)
# Optional system packages: libreoffice (soffice), tesseract-ocr
PySide6>=6.6
PyMuPDF>=1.24
Pillow>=10.0
img2pdf>=0.5
openpyxl>=3.1
python-docx>=1.1
python-pptx>=0.6
pandas>=2.0
pytest>=8.0
# Dev / packaging (optional):
# pyinstaller>=6.0
# pytesseract>=0.3  # optional helper; Morphix also calls tesseract CLI
